import React from 'react';

const collegeFirstName = () => {
    const firstName = [''];
    return firstName[Math.floor(Math.random() * (firstName.length - 1))];
};

const FirstClause = () => {
    return <div></div>;
};

export default FirstClause;
